# Merged with Nitro's generated config at build time (.output/server/wrangler.json).
# Customize the project name here; other deploy settings are generated by the build.
name = "caliber-portfolio"
compatibility_date = "2026-06-10"
compatibility_flags = ["nodejs_compat"]
